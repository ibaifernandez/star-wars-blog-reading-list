import React from "react";

const Search = () => {
    return (
        <h1 className="h1 text-center mt-3">
            These are the results on the query:
        </h1>
    );
};

export default Search;
